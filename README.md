# PusjWorkspace

Dev-оркестратор для монорепо PUSJ. Управляет запуском и разработкой всех компонентов системы в единой Docker-сети.

## 📋 Что это?

PusjWorkspace - это централизованный инструмент для разработки, который:

- Оркестрирует запуск всех сервисов (Core API и Next.js приложение)
- Настраивает единую Docker-сеть для взаимодействия между сервисами
- Упрощает процесс разработки через Makefile команды
- Поддерживает GitHub Codespaces через devcontainer

## 🏗️ Архитектура

```
PusjWorkspace/
├── PusjCore/          # FastAPI backend (git submodule)
├── PusjNext/          # Next.js frontend (git submodule)
├── MasterDocs/        # Документация (git submodule)
└── docker-compose.dev.yml
```

### Сервисы

- **Core** (`pusj-core-dev`): FastAPI приложение на порту 8000
  - Healthcheck: `/health`
  - Внутри Docker-сети доступен как `http://core:8000`

- **Next** (`pusj-next-dev`): Next.js приложение на порту 3000
  - Зависит от Core (ждет его готовности)
  - Использует `http://core:8000` для API запросов внутри сети

## 🚀 Быстрый старт

См. [QUICKSTART.md](./QUICKSTART.md) для детальных инструкций.

```bash
make init && make up
```

## 📦 Команды Makefile

| Команда | Описание |
|---------|----------|
| `make init` | Инициализирует подмодули и создает конфигурационные файлы |
| `make up` | Запускает все сервисы |
| `make down` | Останавливает все сервисы |
| `make logs` | Показывает логи всех сервисов |
| `make status` | Показывает статус сервисов |
| `make rebuild` | Пересобирает и перезапускает сервисы |
| `make clean` | Останавливает сервисы и удаляет volumes |

## 🔧 Настройка

### Git Submodules

1. Скопируйте `.gitmodules.example` в `.gitmodules`
2. Обновите URLs репозиториев на ваши:
   ```bash
   cp .gitmodules.example .gitmodules
   # Отредактируйте .gitmodules
   ```

3. Инициализируйте подмодули:
   ```bash
   make init
   # или
   bash init-submodules.sh
   ```

### Переменные окружения

Если нужны переменные окружения, создайте `.env.example` и `.env`:

```bash
# .env.example
CORE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## 🐳 Docker Compose

Основной файл конфигурации: `docker-compose.dev.yml`

### Особенности

- **Единая сеть**: Все сервисы в сети `pusj-dev-network`
- **Volumes**: Отдельные volumes для зависимостей (venv, node_modules)
- **Healthchecks**: Core проверяется перед запуском Next
- **Hot reload**: Изменения в коде автоматически применяются

### Внутренняя маршрутизация

Внутри Docker-сети сервисы обращаются друг к другу по именам:

- Next → Core: `http://core:8000` (НЕ localhost!)
- Снаружи: `http://localhost:8000` и `http://localhost:3000`

## 💻 GitHub Codespaces

Workspace настроен для работы в Codespaces:

- `.devcontainer/devcontainer.json` - конфигурация контейнера
- `.devcontainer/post-create.sh` - автоматическая настройка при создании

При открытии в Codespaces автоматически:
1. Инициализируются подмодули
2. Создаются необходимые файлы
3. Настраивается окружение

## 📁 Структура файлов

```
.
├── docker-compose.dev.yml    # Docker Compose конфигурация
├── Makefile                  # Команды для управления
├── .gitmodules.example       # Пример конфигурации подмодулей
├── init-submodules.sh        # Скрипт инициализации подмодулей
├── .devcontainer/
│   ├── devcontainer.json     # Конфигурация Codespaces
│   └── post-create.sh        # Скрипт пост-создания
├── QUICKSTART.md             # Быстрый старт
└── README.md                 # Этот файл
```

## 🔒 Безопасность

- **Никаких секретов в репозитории**
- Используйте `.env` для локальных переменных (не коммитьте!)
- `.env.example` - шаблон для документации

## 🤝 Разработка

### Добавление нового сервиса

1. Добавьте сервис в `docker-compose.dev.yml`
2. Обновите Makefile при необходимости
3. Обновите документацию

### Отладка

```bash
# Логи конкретного сервиса
docker-compose -f docker-compose.dev.yml logs -f core
docker-compose -f docker-compose.dev.yml logs -f next

# Вход в контейнер
docker exec -it pusj-core-dev bash
docker exec -it pusj-next-dev bash
```

## 📝 Лицензия

[Укажите лицензию]

## 👥 Авторы

[Укажите авторов]
