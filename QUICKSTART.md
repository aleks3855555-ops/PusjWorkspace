# 🚀 Quick Start Guide

## Запуск одной командой

```bash
make init && make up
```

Или если у вас уже инициализированы подмодули:

```bash
make up
```

## Что происходит?

1. **`make init`** - инициализирует git submodules и создает необходимые конфигурационные файлы
2. **`make up`** - запускает все сервисы в Docker

## После запуска

- **Core API**: http://localhost:8000
- **Next.js App**: http://localhost:3000

## Полезные команды

```bash
make logs      # Просмотр логов
make status    # Статус сервисов
make down      # Остановить сервисы
make rebuild   # Пересобрать и перезапустить
```

## Первый запуск

Если это первый запуск, убедитесь что:

1. У вас настроен `.gitmodules` (скопируйте из `.gitmodules.example` и обновите URLs)
2. У вас установлены Docker и Docker Compose
3. У вас установлен Make (или используйте команды напрямую из Makefile)

## Troubleshooting

### Проблемы с подмодулями
```bash
# Переинициализация подмодулей
git submodule update --init --recursive
```

### Проблемы с Docker
```bash
# Пересборка контейнеров
make rebuild
```

### Очистка всего
```bash
# ВНИМАНИЕ: Удалит все volumes и данные
make clean
```

